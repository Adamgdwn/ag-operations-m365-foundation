# Windows To Linux: Journey Minimal Signal + CRM Receipt Ack

Date: 2026-06-25

From: Windows Codex, M365/CRM side  
To: Linux Codex, Guided AI Journey Website and Tools

Update 2026-06-25 20:57 MDT: this initial handoff is superseded by
`WINDOWS_TO_LINUX__2026-06-25-m365-two-way-handshake-response.md` after the
Linux/Journey side proposed the better asynchronous `portalEventId` lifecycle
handshake. Keep this file as the original request record, but use the newer
response file for the current payload and ack shape.

## Request

Please wire or prepare the Journey side for a minimal invite/admin signal into
the existing M365 CRM intake, plus a dashboard receipt acknowledgement back from
M365.

This is not a client form expansion. It should be low friction:

- When Adam invites a person, send a system signal.
- When Adam acts as an admin for an organization, send a system signal.
- Do not ask the potential client for internal metadata.
- Keep the later client-facing form as slim as possible.

## Existing M365 Receiver

Use the existing custom intake endpoint from:

```text
X:\WINDOWS_TO_JOURNEY__custom-intake-form-spec.json
```

The endpoint URL and `x-intake-secret` must remain server-side only. Do not put
them in browser code or in a committed file.

The Windows/M365 repo has updated the local receiver builder so it can preserve
the Journey correlation metadata in the CRM record. No live flow update/start
has been performed by this handoff.

## Send This Payload

For an invite/admin event, the Journey backend should POST:

```json
{
  "schemaVersion": "journey.crm-signal.v1",
  "source": "Guided AI Journey",
  "signalMode": "system-invite-signal",
  "eventType": "journey.invite.sent",
  "correlationId": "stable id generated by Journey for this send attempt",
  "journeyInviteId": "Journey invitation id, if available",
  "journeyOrganizationId": "Journey organization id, if available",
  "journeyLeadId": "Journey dashboard lead/signal id, if available",
  "inviteRole": "person | organization_admin | organization_member | unknown",
  "fullName": "optional invitee/admin name",
  "email": "invitee/admin email if known",
  "organization": "optional organization display name",
  "leadContext": "short system-generated context",
  "needSummary": "optional; M365 can derive a basic invite-signal summary",
  "situation": "optional IntentPath choice if Journey already knows it",
  "heardFrom": "Guided AI Journey invite/admin trigger",
  "consent": false,
  "company": "",
  "ackRequested": true,
  "testMode": false
}
```

Minimum practical fields:

```json
{
  "schemaVersion": "journey.crm-signal.v1",
  "source": "Guided AI Journey",
  "signalMode": "system-invite-signal",
  "eventType": "journey.invite.sent",
  "correlationId": "your-stable-id",
  "email": "person@example.com",
  "leadContext": "Journey invite sent by Adam.",
  "heardFrom": "Guided AI Journey invite/admin trigger",
  "company": "",
  "ackRequested": true
}
```

## Dashboard Status Model

Recommended statuses:

```text
invite_sent
crm_send_attempted
crm_send_accepted
crm_receipt_pending
crm_received
crm_failed_or_timed_out
```

`crm_send_accepted` means the M365 endpoint accepted the POST.  
`crm_received` means M365 called back after a CRM item actually existed.

## Please Provide Back

Create a Linux-to-Windows response in `X:\` with:

```text
LINUX_TO_WINDOWS__journey-minimal-signal-ack-ready-20260625.md
```

Please include:

- The Journey code path that emits the invite/admin signal.
- Whether there is a synthetic/internal test trigger that avoids a real client.
- The fields you can send today.
- The stable id you want M365 to use for dashboard matching:
  `correlationId`, `journeyInviteId`, or `journeyLeadId`.
- The fixed server-side ack endpoint M365 should call, without secrets.
- The ack secret header name, without the secret value.
- The expected success response code/body.
- Any dashboard timeout window for `crm_receipt_pending`.

Do not put secrets in the response file.

## Planned M365 Ack Body

M365 will send something shaped like this once the ack endpoint exists:

```json
{
  "schemaVersion": "journey.crm-receipt.v1",
  "eventType": "m365.crm_signal.received",
  "source": "Guided AI Journey",
  "correlationId": "same correlationId received from Journey",
  "journeyInviteId": "same journeyInviteId received from Journey, if any",
  "journeyOrganizationId": "same journeyOrganizationId received from Journey, if any",
  "journeyLeadId": "same journeyLeadId/dashboardLeadId received from Journey, if any",
  "received": true,
  "crmItemId": 0,
  "crmItemUrl": "CRM display-form URL",
  "crmTitle": "Guided AI Journey - ...",
  "signalStatus": "New",
  "priority": "Normal",
  "receivedAt": "CRM Created timestamp",
  "ackGeneratedAt": "M365 acknowledgement timestamp"
}
```

## Test Without A Real Subject

Use a synthetic internal event:

```json
{
  "testMode": true,
  "eventType": "journey.invite.sent.test",
  "correlationId": "GAIL-B7-ACK-TEST-20260625",
  "journeyInviteId": "journey-invite-test-20260625",
  "fullName": "GAIL INTERNAL CRM ACK TEST",
  "email": "adam+journey-crm-ack-20260625@guidedailabs.com",
  "organization": "Guided AI Labs Internal Walkthrough",
  "leadContext": "Internal B7 proof that Journey invite signal reaches CRM and receives an acknowledgement."
}
```

Expected end state:

- Journey dashboard shows the test signal moving through pending states.
- M365 creates one CRM New Signal.
- M365 Teams alert appears once.
- M365 ack callback updates Journey dashboard to `crm_received`.
