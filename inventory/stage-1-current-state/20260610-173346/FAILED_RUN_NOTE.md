This inventory run failed during REST device-code authentication polling.

The script was patched afterward to support PowerShell 7 HTTP response parsing.

Use a later successful inventory folder instead.
